begin
 button = KEY_0
 prog = mpd_remote
 config = KEY_0
end

begin
 button = KEY_1
 prog = mpd_remote
 config = KEY_1
end

begin
 button = KEY_2
 prog = mpd_remote
 config = KEY_2
end

begin
 button = KEY_3
 prog = mpd_remote
 config = KEY_3
end

begin
 button = KEY_4
 prog = mpd_remote
 config = KEY_4
end

begin
 button = KEY_5
 prog = mpd_remote
 config = KEY_5
end

begin
 button = KEY_6
 prog = mpd_remote
 config = KEY_6
end

begin
 button = KEY_7
 prog = mpd_remote
 config = KEY_7
end

begin
 button = KEY_8
 prog = mpd_remote
 config = KEY_8
end

begin
 button = KEY_9
 prog = mpd_remote
 config = KEY_9
end

begin
 button = KEY_MINUS
 prog = mpd_remote
 config = KEY_MINUS
end

begin
 button = KEY_VOLUMEDOWN
 prog = mpd_remote
 config = KEY_VOLUMEDOWN
end

begin
 button = KEY_VOLUMEUP
 prog = mpd_remote
 config = KEY_VOLUMEUP
end

begin
 button = KEY_ENTER
 prog = mpd_remote
 config = KEY_ENTER
end

begin
 button = KEY_EQUAL
 prog = mpd_remote
 config = KEY_EQUAL
end

begin
 button = KEY_PREVIOUS
 prog = mpd_remote
 config = KEY_PREVIOUS
end

begin
 button = KEY_NEXT
 prog = mpd_remote
 config = KEY_NEXT
end

begin
 button = KEY_PLAYPAUSE
 prog = mpd_remote
 config = KEY_PLAYPAUSE
end

begin
 button = KEY_POWER
 prog = mpd_remote
 config = KEY_POWER
end

begin
 button = KEY_MODE
 prog = mpd_remote
 config = KEY_MODE
end

begin
 button = KEY_MUTE
 prog = mpd_remote
 config = KEY_MUTE
end
